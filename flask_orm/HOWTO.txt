WIN:
pg_ctl -D "C:\Program Files\PostgreSQL\11\data" start
LINUX:
sudo service postgresql start
sudo passwd postgres
su postgres
ALTER USER postgres PASSWORD 'MyNewPassword';


\l : list of available databases
\du : list of users
\q : quit


psql -h localhost -U postgres -p 5433
postgres=# create database flask_practice;
postgres=# create user aibragimov with encrypted password '...';
postgres=# grant all privileges on database flask_practice to aibragimov;
postgres=# ALTER USER aibragimov WITH SUPERUSER;
postgres=# \q;


set up postgres db
add data
create migrations
connect db to app
one-to-one
one-to-many
many-to-many


+pycryptodom

import base64
from flask_restful.utils.crypto import encrypt, decrypt
# encrypt(new_bookmark, key, seed)
#
# def decrypt(w):
#     return base64.b64encode(w.encode('utf-8'))
#
#
# def encrypt(w):
#     return base64.b64decode(w).decode('utf-8')
key = b'0123456789abcdef0123456789abcdef'
seed = b'0123456789abcdef'
print(decrypt(b'very_secret_password', key=key, seed=seed))
